import { TeamPageSettings } from '../../components/manage';

/** Page tab of /manage/team — the team page's layout options (self-notifies on save). */
export const TeamPageTab: React.FC = () => <TeamPageSettings />;
