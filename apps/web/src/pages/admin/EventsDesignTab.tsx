import { EventsDesignEditor } from '../../components/manage/events';

/** Design tab of /manage/events — agenda + event page options (self-notifies on save). */
export const EventsDesignTab: React.FC = () => <EventsDesignEditor />;
