import { GalleryTagsEditor } from '../../components/manage/gallery';

/** Tags tab of /manage/gallery — the tag lifecycle (self-notifies on save). */
export const GalleryTagsTab: React.FC = () => <GalleryTagsEditor />;
