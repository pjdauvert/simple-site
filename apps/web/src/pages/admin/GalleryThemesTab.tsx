import { GalleryThemesEditor } from '../../components/manage/gallery';

/** Themes tab of /manage/gallery — themes and their images (self-notifies on save). */
export const GalleryThemesTab: React.FC = () => <GalleryThemesEditor />;
