import { TeamDesignEditor } from '../../components/manage';

/** Design tab of /manage/team — per-surface design options (self-notifies on save). */
export const TeamDesignTab: React.FC = () => <TeamDesignEditor />;
