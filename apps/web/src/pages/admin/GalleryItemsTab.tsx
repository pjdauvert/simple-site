import { GalleryItemsEditor } from '../../components/manage/gallery';

/** Items & tags tab of /manage/gallery — the items and their tags (self-notifies on save). */
export const GalleryItemsTab: React.FC = () => <GalleryItemsEditor />;
