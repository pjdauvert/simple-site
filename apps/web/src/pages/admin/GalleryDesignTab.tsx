import { GalleryDesignEditor } from '../../components/manage/gallery';

/** Design tab of /manage/gallery — display mode + caption position (self-notifies on save). */
export const GalleryDesignTab: React.FC = () => <GalleryDesignEditor />;
