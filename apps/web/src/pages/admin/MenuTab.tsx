import { MenuEditor } from '../../components/manage';

/** Menu tab of /manage/site — navigation ordering & visibility (self-notifies on save). */
export const MenuTab: React.FC = () => <MenuEditor />;
