import { TeamMembersEditor } from '../../components/manage';

/** Members tab of /manage/team — the member list editor (self-notifies on save). */
export const TeamMembersTab: React.FC = () => <TeamMembersEditor />;
