import { EventsListEditor } from '../../components/manage/events';

/** List tab of /manage/events — the event list (self-notifies on save). */
export const EventsListTab: React.FC = () => <EventsListEditor />;
