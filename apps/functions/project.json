{
  "name": "functions",
  "projectType": "application",
  "sourceRoot": "apps/functions/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/apps/functions"],
      "dependsOn": ["^build"],
      "options": {
        "command": "tsc -p apps/functions/tsconfig.build.json"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "tsc -p apps/functions/tsconfig.json --noEmit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config apps/functions/vitest.config.ts"
      }
    }
  },
  "implicitDependencies": ["interfaces"],
  "tags": []
}
